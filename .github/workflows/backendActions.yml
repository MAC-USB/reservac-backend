name: backend Actions

on:
  push:
    branches: [ "master" ]
  
jobs:

  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    - name: prueba
      working-directory: /root/services/reserva/backend/
      run: |
        ls -a
        pwd
